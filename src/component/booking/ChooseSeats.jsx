function ChooseSeats({ listSeats }) {
    return (
        <div>
            {listSeats.map((seat, index) => {
                {
                    if(index % 10 === 0){

                    }
                }
                
            })}
        </div>
    );
}
export default ChooseSeats;
